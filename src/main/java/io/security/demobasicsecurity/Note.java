package io.security.demobasicsecurity;

import io.security.demobasicsecurity.인증개념이해_Authentication.SecurityConfig9;
import io.security.demobasicsecurity.인증저장소_SecurityContextHolder_SecurityContext.SecurityConfig10;
import io.security.demobasicsecurity.필터초기화및다중보안설정.SecurityConfig8;

public class Note
{

    // 필터 초기화 및 다중 보안 설정 하는 방법
    SecurityConfig8 SecurityConfig8 = new SecurityConfig8();

    // 사용자의 인증 정보를 저장하는 토큰 개념 (Authentication)
    SecurityConfig9 SecurityConfig9 = new SecurityConfig9();

    // 인증 저장소 (SecurityContextHolder, SecurityContext)
    SecurityConfig10 SecurityConfig10 = new SecurityConfig10();
}
