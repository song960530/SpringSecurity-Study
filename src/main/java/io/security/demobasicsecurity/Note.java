package io.security.demobasicsecurity;


import io.security.demobasicsecurity.인증저장소_SecurityContextHolder_SecurityContext.SecurityConfig;

public class Note
{

    // 필터 초기화 및 다중 보안 설정 하는 방법
    io.security.demobasicsecurity.필터초기화및다중보안설정.SecurityConfig SecurityConfig8 = new io.security.demobasicsecurity.필터초기화및다중보안설정.SecurityConfig();

    // 사용자의 인증 정보를 저장하는 토큰 개념 (Authentication)
    io.security.demobasicsecurity.인증개념이해_Authentication.SecurityConfig SecurityConfig9 = new io.security.demobasicsecurity.인증개념이해_Authentication.SecurityConfig();

    // 인증 저장소 (SecurityContextHolder, SecurityContext)
    io.security.demobasicsecurity.인증저장소_SecurityContextHolder_SecurityContext.SecurityConfig SecurityConfig10 = new SecurityConfig();

    // 인증저장소필터_SecurityContextPersistenceFilter
    io.security.demobasicsecurity.인증저장소필터_SecurityContextPersistenceFilter.SecurityConfig SecurityConfig11 = new io.security.demobasicsecurity.인증저장소필터_SecurityContextPersistenceFilter.SecurityConfig();
}
